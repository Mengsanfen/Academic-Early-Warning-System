# 学业预警系统 - 快速启动指南

## 环境要求

| 环境 | 版本要求 |
|------|----------|
| Python | 3.8+ |
| Node.js | 16+ |
| MySQL | 5.7+ / 8.0 |

---

## 启动步骤

### 第一步：启动后端

打开终端，执行以下命令：

```bash
cd D:\workspace\new_yujing\Academic-Early-Warning-System-1.0.1\backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 第二步：启动前端

打开**新的**终端，执行以下命令：

```bash
cd D:\workspace\new_yujing\Academic-Early-Warning-System-1.0.1\frontend_new
npm run dev
```

---

## 访问地址

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:5173 |
| 后端 API | http://localhost:8000 |
| API 文档 | http://localhost:8000/docs |

---

## 测试账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 辅导员 | counselor | counselor123 |

---

## 首次运行

如果是首次运行项目，需要先安装依赖：

### 安装后端依赖
   
```bash
cd D:\workspace\new_yujing\Academic-Early-Warning-System-1.0.1\backend
pip install -r requirements.txt
```

### 安装前端依赖

```bash
cd D:\workspace\new_yujing\Academic-Early-Warning-System-1.0.1\frontend_new
npm install
```

---

## 数据库配置

修改 `backend/.env` 文件：

```env
DATABASE_URL=mysql+pymysql://root:你的密码@localhost:3306/alert_system
```

创建数据库：

```sql
CREATE DATABASE alert_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

## 常见问题

**端口被占用：**

```bash
# 查看 8000 端口
netstat -ano | findstr :8000

# 查看 5173 端口
netstat -ano | findstr :5173
```

**依赖安装慢：**

```bash
# Python 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# npm 使用国内镜像
npm install --registry=https://registry.npmmirror.com
```